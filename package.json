{
  "name": "zinxi",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "zig build",
    "cbuild": "gcc src/c/*.c -o test  -o test -lpci -lcpuid -lOpenCL",
    "test": "bun --bun run build && ./zig-out/bin/zinxi",
    "ctest": "bun --bun run cbuild && ./test",
    "dev": "bun --bun watcher.js",
    "cdev": "bun --bun watcher.js --c"
  }
}
